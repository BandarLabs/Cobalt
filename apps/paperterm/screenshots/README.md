# Paperterm screenshots

- `pairing.png`: fresh portrait app, captured through the committed drive route.
- `terminal.png`: Clara BW portrait at Default, sharing a real host PTY over
  privately trusted TLS. Reader input and laptop input appear in the same
  session. The terminal is 75 columns wide; opening the keyboard leaves 25 rows.

These are actual SDK simulator captures, not mockups. Matching provenance,
layouts and the normal/large/Elipsa results live under
`docs/quality/evidence/paperterm`. Physical Clara BW acceptance is still pending.

`input-paused.png` is the actual Extra-large recovery screen after an injected
key-request timeout. Evidence is in `docs/quality/evidence/paperterm/input-recovery`.


The current welcome, preview, three setup steps, address and code-error captures
come from the committed offline route. Its fetch/post counters remain zero.
`terminal.png`, `input-paused.png` and `reconnecting.png` are refreshed from the
live status/recovery journeys. Current evidence is under `onboarding`,
`paired-live` and `status-default`; earlier evidence folders retain earlier
versions and their historical row counts.
