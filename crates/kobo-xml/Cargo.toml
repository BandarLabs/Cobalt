[package]
name = "kobo-xml"
version.workspace = true
edition.workspace = true
publish = false
rust-version.workspace = true
license.workspace = true

# Deliberately empty, same reasoning as kobo-json: this is a scanner over
# bytes a stranger's server sent, not a place to hang a parsing framework, and
# every crates.io dependency in this workspace lives in kobo-net alone.
[dependencies]

[lints]
workspace = true
