This is a public, deterministic test key for `kobo beta-store-smoke`.
It is not a release credential. The mock harness builds signed baseline,
target, downgrade, malformed, interrupted, and launch-failure packages with
the production catalog and bundle code paths.

`marketing-route.txt` names public-only title, interaction, and result beats.
Its coordinates drive only the deterministic mock route.
